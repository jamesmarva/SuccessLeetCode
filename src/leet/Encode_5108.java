package leet;

import java.math.BigDecimal;
import java.math.BigInteger;

/**
 * @author James
 * @date 2019-11-16 22:37
 **/
public class Encode_5108 {
    public String encode(int num) {
        return null;
    }

    public static void main(String[] args) {
        BigInteger bi = new BigInteger(String.valueOf("23")); // 转换成 BigInteger 类型，默认是十进制
        System.out.println(bi.toString(2));// 参数 2 指定的是转化成二进制
        BigInteger bi1 = new BigInteger(String.valueOf("107")); // 转换成 BigInteger 类型，默认是十进制
        System.out.println(bi1.toString(2));// 参数 2 指定的是转化成二进制
    }
}
